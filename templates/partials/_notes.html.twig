<div class="row mt-5">
    {% for item in note %}
        <div class="shadow col-md-4 mb-4">
            <div class="card card-bg rounded-4">
                <div class="card-body">
                    <h5 class="card-title">{{ item.title }}</h5>
                    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                    <a href="#" class="btn btn-gradient">Voir la note</a>
                </div>
            </div>
        </div>
    {% else %}
        <div class="col-md-12">
            <div class="alert alert-warning" role="alert">
                <h4 class="alert-heading">Aucune note</h4>
                <p>Il n'y a aucune note pour le moment.</p>
                <hr>
                <p class="mb-0">Vous pouvez en créer une en cliquant sur le bouton ci-dessous.</p>
                <a href="" class="btn btn-gradient mt-3">Créer une note</a>
            </div>
        </div>
    {% endfor %}
    <div class="col-md-12 text-center mb-4">
        <a class="btn btn-warning" href={{ path('app_note') }}>Voir toutes les notes</a>
    </div>
</div>